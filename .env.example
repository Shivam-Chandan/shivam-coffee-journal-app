# Environment Configuration Example

# Node environment
NODE_ENV=development

# GCP Project ID
GCP_PROJECT_ID=your-project-id

# Firestore Database Name (optional - defaults to default database)
# If you have a custom Firestore database, specify its name here
FIRESTORE_DATABASE_NAME=coffee

# Firebase Configuration (only needed for local development with Firestore)
# Get this from your GCP service account key (the entire JSON as a string)
# FIREBASE_CONFIG={"type":"service_account","project_id":"..."}

# Port configuration
# PORT=5000
